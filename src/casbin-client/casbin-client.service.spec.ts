/* import { Test, TestingModule } from '@nestjs/testing';
import { CasbinClientService } from './casbin-client.service';

describe('CasbinClientService', () => {
  let service: CasbinClientService;

  beforeEach(async () => {
    const module: TestingModule = await Test.createTestingModule({
      providers: [CasbinClientService],
    }).compile();

    service = module.get<CasbinClientService>(CasbinClientService);
  });

  it('should be defined', () => {
    expect(service).toBeDefined();
  });
});
 */